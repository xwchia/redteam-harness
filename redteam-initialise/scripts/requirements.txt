# Core dependencies to run the red-team pipeline skills and the in-repo scripts.
# These cover: redteam_client.py (Azure OpenAI), redteam-campaign/redteam-consolidate/
# redteam-report scripts, the redteam-autoresearch skill scripts, and the prep harness libs.

# Target client + config + env loading
openai>=1.30
python-dotenv>=1.0
pyyaml>=6.0

# HTTP + async + retries (client wrappers, rate-limit handling)
httpx>=0.27
aiohttp>=3.9
requests>=2.31
tenacity>=8.2

# Progress + console output + results handling
tqdm>=4.66
rich>=13.7
pandas>=2.0
